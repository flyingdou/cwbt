<!-- 页面开始 -->
<view class = 'content'>
      <!-- 搜索框 -->
      <view class='searchDiv'>
            <view class='search'>
                  <input type='text' placeholder='搜索' bindinput='inputChange' data-key = 'searchName'></input>
                  <view class='searchIcon' bindtap='search'>
                        <image src='../../icon/search.png' mode='aspectFill'></image>
                  </view>
            </view>
      </view>
      
      <!-- 导航栏 -->
      <view class = 'navBar'>
            <view class='preNav' wx:for = '{{navList}}' wx:key = '' wx:for-item='nav' bindtap='up' data-index = '{{index}}' >
                  <view class='nav {{index == (navList.length-1) ? "current" : ""}}' data-deptid='{{nav.seq_id}}'>{{nav.dept_name}}</view>
                  <text wx:if = '{{index < (navList.length-1)}}' class='nav' decode='ture'>&gt;</text>
            </view>
      </view>

      
      <!-- 下级组织机构 -->
      <view wx:if = '{{deptList && deptList.length > 0}}' class='deptList'>
            <!-- preDept -->
            <view class='preDept' wx:for = '{{deptList}}' wx:for-item = 'dept' wx:key = ''>

                  <view class='left' bindtap = 'chooseDept' data-deptindex = '{{index}}'>
                        <view class='deptImage'>
                              <image src='{{base_img_url}}/shipLogo1.jpg'></image>
                        </view>
                        <checkbox checked='{{dept.checked}}'  color = 'rgba(54, 123, 240, 1)'></checkbox>
                       
                        <view class='deptName'>{{dept.dept_name}}</view>
                        <view class='personCount'>（{{dept.personCount}}）</view>
                  </view>

                  <view class='right' bindtap='next' data-dept = '{{dept}}' data-deptindex = '{{index}}'>
                          <view class='next'>
                                <view class='nextImg'>
                                      <image src='../../icon/peoples.png' mode='aspectFill'></image>
                                </view>
                                <view class='nextFont'>下级</view>
                          </view>
                    </view>

            </view>

      
      </view>


      <!-- 当前机构的人员 -->
      <view class='userList' wx:if = '{{userList && userList.length > 0}}'>
            <!-- checkboxGroup -->
            <checkbox-group>
                    <view class='preUser' wx:for = '{{userList}}' wx:for-item = 'user' wx:key = '' bindtap='checkboxChange' data-index = '{{index}}'>

                          <view class='left'>
                                <view class='userImage'>彬</view>
                                <view class='userName'>{{user.user_name}}</view>
                          </view>
                          <view class='checkBox'>
                                <checkbox checked='{{user.checked}}' color='rgba(54, 123, 240, 1'></checkbox>
                          </view>
                    </view>
            </checkbox-group>
      </view>

      <!-- fill -->
      <view class='douFill'></view>

      <!-- footer -->
      <view class='footer'>
            <view class='chooseAllLine' bindtap='chooseAll'>
                  <view class='chooseAll'>全选</view>
                  <view class='checkBox'>
                        <checkbox checked='{{checkAll}}'  color='rgba(54, 123, 240, 1'></checkbox>
                  </view>
            </view>

            <view class='sureLine'>
                  <view class='title'>已选择:<text wx:if = '{{(indexs && indexs.length > 0) || (deptUser && deptUser.length)}}'>{{(indexs.length || 0) + (deptUser.length || 0)}}人</text></view>
                  <view class='sure' bindtap='choose'>确定</view>
            </view>
      </view>



</view>
